package model;

/* This is a class dedicated to holding the information about the Pay Rates */
public class PayRate extends Table{
	public PayRate(int numOfRows, String[] fieldNames, String[][] data) {
		super(numOfRows, fieldNames, data);
	}
}
